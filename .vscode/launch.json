{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch and Debug Standalone Blazor WebAssembly App (HTTP)",
            "type": "blazorwasm",
            "request": "launch",
            "cwd": "${workspaceFolder}/clypse.portal"
        }
    ]
}
